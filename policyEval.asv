function [values] = policyEval(policy, actions, discFactor)
    Ns = length(policy(1));
    Na = length(actions);
    
    V = np.zeros(Ns);
    delta = 0;
    endCond = 0.0001;
    
    while delta > endCond
       delta = 0;
       V_new = np.zeros(Ns);
       
       for s = 1:Ns
           actionProbs = policy(:, s);
           for action = 1:Na
               V_new(s) = V_new(s) + actionProbs(acti
           end
       end
    end
end